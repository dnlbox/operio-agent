operio.chat, www.operio.chat {
    # Reverse proxy public requests to the FastAPI application
    reverse_proxy /api/* app:3001
    reverse_proxy app:3001
}

phoenix.operio.chat {
    # Reverse proxy requests to Arize Phoenix
    reverse_proxy phoenix:6006
}
